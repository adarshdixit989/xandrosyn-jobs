# Firebase Android setup

1. Download the Android Firebase config for package `com.xandrosyn.jobs` from your Firebase project.
2. Save it as `mobile/android/app/google-services.json`.
3. The real file is ignored by Git and should not be committed to the public repository.
4. Keep server-side Firebase Admin credentials separate; do not use this mobile config as server credentials.
